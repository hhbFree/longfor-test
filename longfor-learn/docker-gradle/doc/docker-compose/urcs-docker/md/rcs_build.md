# 各个服务绑定本地端口
## SBC 
```
# INNER内部服务调用，OUTER外部服务调用

# SIP 协议端口
SBC_SIP_OUTER_TCP_PORT=5260
SBC_SIP_OUTER_UDP_PORT=5060
SBC_SIP_OUTER_TLS_PORT=5061
SBC_SIP_INNER_TCP_PORT=6070

# MSRP 服务端口
MSRP_INNER_TCP_PORT=30000
MSRP_OUTER_TCP_PORT=20000
MSRP_OUTER_TLS_PORT=20001

# RTP 服务端口
RTP_INNER_PORT_START=20000
RTP_INNER_PORT_END=30000
RTP_OUTER_PORT_START=30000
RTP_OUTER_PORT_END=40000

# STUN 服务端口
STUN_PORT=10443

# RX 端口
RX_PORT=3868
```

## CSCF
```
# CSCF服务绑定端口
CSCF_PORT=6060

# Cx 端口
CX_PORT=16061
```

## IM
```
# SIP协议端口
IM_SIP_PORT=10081

# im http端口
IM_HTTP_PORT=9010

# MSRP协议端口
MSRP_PORT=20050
```

## TAS
```
# SIP 端口
TAS_SIP_PORT=10006

# http 端口
TAS_HTTP_PORT=9012
```

## GROUP
```
# 群服务端口
SBC_SIP_INNER_TCP=sip-inner-tcp

# SIP 端口
SBC_SIP_INNER_TCP_PORT=6071

# MSRP 端口
MSRP_PORT=30020

# HTTP 端口
GROUP_HTTP_PORT=9014
```

## DM
```
HTTP 9016
HTTPS 9017
```

## MAAP
```
HTTP 8099
```